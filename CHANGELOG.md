# 更新日志

> 当前正式命名与安装路径已经统一为：`zwrt-datad`、`/data/plugins/zwrt-datad/zwrt-datad`、`/data/plugins/u60pro-devui/`、`/data/plugins/u60pro-devui/ui`。历史条目里如果出现 `u60-datad`、`/data/u60pro` 或 `/data/ui`，表示当时版本记录。

## v1.2.12-remix.6 - 2026-07-19

### 修复

- WiFi 节能模式改由 DevUI 单独持久化到 `wifi-power-save.conf`，统一使用 `99-devui-wifi-powersave` 在接口启动时恢复。
- 首次启动清理旧 `psm`、`99-disable-powersave` 和 `psm_boot` 自启行，避免“WiFi 节能控制”插件残留把节能模式重新覆盖为高性能。
- 修正状态读取：U60Pro 的 `wlan0` 驱动拒绝 `power_save on`，改以支持 PSM 的 `wlan2/wlan3` 为准；发现状态被外部规则改写时，按保存状态限频复核并恢复。

### 新增

- 双卡 U60Pro 的锁频子页新增“自插卡 / 内置卡”切换，仅在 `support_dual_sim=1` 时显示；切卡需要 4 秒内二次确认，并通过真实槽位回读显示成功或失败。

## v1.2.12-remix.5 - 2026-07-18

### 变更

- 第三页四张图分别支持 `30s / 48s / 1min / 2min / 5min` 固定时间窗口，设置独立保存。
- 图表历史改为 301 个 monotonic 时间戳环形样本，页面刷新、当前页面和熄屏不再决定是否采样。
- datad 新增只读取缓存快照的 `/chart-metrics`，熄屏时继续低资源记录历史。
- Tailscale、Mihomo、WireGuard 必须在同一候选目录同时具备可读控制器和可执行核心二进制才显示。
- 漫游锁卡必须在同一目录具备 `operatorctl.sh` 和 `config.json`；CPU 页面必须具备可读控制器和可写 cpufreq 节点。

### 修复

- 折线改为时间轴定位和覆盖率抗锯齿，减少 RGB565 上的毛刺与方块感。
- 允许一次主循环调度漏秒，避免正常重绘被误判为图表断点；连续缺失仍保留真实空白。
- 发布打包强制校验五个插件功能页面，避免 UI 包缺页。

## v1.2.12-remix.4 - 2026-07-18

### 修复

- WireGuard 功能入口现在同时要求 `wgctl.sh` 与插件自带的 `bin/wg` 可执行，完全卸载后遗留的配置和控制器骨架不再被误判为已安装。
- WireGuard 页面打开、状态刷新和服务操作统一使用相同的完整安装判断。

## v1.2.12-remix.3 - 2026-07-17

### 变更

- 插件控制器优先从 `/data/plugins` 读取，并兼容 Tailscale、Mihomo、WireGuard 和漫游锁卡的历史目录。
- 新增 WireGuard 客户端页面，支持启停、重启、刷新和紧凑 Peer 状态。
- 新增漫游卡锁运营商页面，支持后台状态刷新、扫描、选择后二次确认锁定、恢复自动选网和取消任务。
- 插件状态改为只刷新当前页面，漫游锁卡空闲时降为 10 秒轮询，避免耗时 AT 查询阻塞主界面。

### 性能

- Peer 和运营商列表使用固定高度紧凑行，复用现有长页缓存、45 FPS 滑动和惯性路径。

## v1.2.12-remix.2 - 2026-07-16

### 修复

- 自定义功能页扫描忽略点文件，避免原版管理器升级时遗留的 macOS `._*.html` 被识别成重复入口。

## v1.2.12-remix.1 - 2026-07-16

### 变更

- 全界面调整为冷灰与电光蓝的 Taste 精密仪表盘风格，同时保留深浅主题和 RGB565 低资源渲染。
- 增加 Tailscale、Clash/Mihomo 和 CPU 性能页面，并按本地控制接口是否存在决定是否显示。
- CPU 控制器可随 `ui.tar.gz` 安装到 `ui/functions/cpuctl.sh`，同时兼容旧路径。
- 发布清单合并 datad、devui 和 UI 三个组件，可直接供原版屏幕管理插件作为自定义源。

### 修复

- 合入上游 `v1.2.12` 的短信关闭触控清理，避免关闭弹窗后遗留事件命中下方短信。

### 性能

- 保留 45 FPS 交互突发、按需手势缓存、黑屏资源释放和纵向惯性滚动优化。

## v1.2.11 - 2026-07-10

### 修复

- **长短信详情滚动**：短信标题、日期和关闭按钮保持固定，仅在正文真实超出裁剪区且手势能改变滚动位置时启用上下滚动；短短信和已到边界的手势不会进入假拖动状态。正文保留原始换行，长文本显示滚动位置提示。
- **短信详情关闭范围**：点击短信正文或弹窗空白处不再关闭详情，只有点击底部“关闭”按钮才会退出。
- **高级设置关闭穿透**：高级设置内的实时抬手和慢帧锁存点击统一由弹层处理，并成对消费对应 tap/stroke，关闭后不再触发下方“高级设置”入口而重新打开。

### 验证

- 已在 Ubuntu 完成 aarch64 musl 静态构建，并部署到 U60 实机；DRM `320x480`、触摸 `event3` 与电源键初始化正常。
- 已在 U60 上用现有短信验证正文布局：短短信正文 4 行、视口高度 `287px` 时 `scroll_max=0`，不再出现滚动条或底部超长空白；弹窗正文与关闭按钮位置正常。
- 发布资产 SHA256：`u60pro-devui-aarch64` 为 `5166aeb8c53c6b436156a194326e495be6e9761496969d96bdc0a6c38e594992`，`ui.tar.gz` 为 `3571d16bc5b2064f1bff77c63aac19cfbb77d81ec80cea4912ff61feb53259c8`。

## v1.2.10 - 2026-07-10

### 修复

- **短信详情关闭点击穿透**：关闭短信详情时会消费同一次触摸锁存的 tap/stroke，避免弹窗消失后把关闭按钮坐标再次投递到下方短信列表，误打开另一条短信；慢帧期间未观察到按下沿的完整 tap 同样由详情弹窗自身消费。

### 验证

- 已在 Ubuntu 交叉编译，并推送到 U60 实机验证：DRM `320x480`、触摸 `event3`、电源键与 datad 状态连接均正常；连续打开、关闭短信不再跳转到另一条。

## v1.2.9 - 2026-07-09

### 变更

- **更多功能二级页**：默认顶层页收敛为信号、更多功能、图表和系统设置；WiFi、短信、信令读取、锁频和可选测速入口迁移到 `02-functions.html` 下的二级页面，减少左右滑动页数。
- **自定义功能入口**：新增 `/data/plugins/u60pro-devui/ui/functions/*.html` 扫描能力，自定义页面会自动出现在“更多功能”页；示例放在 `examples/custom-functions/`，不会默认打进 UI 包。
- **邻小区列表**：第一页新增 `{{NEIGHBORCARDS}}`，信令解析开启时可展开显示 LTE/NR 邻区，每行显示 `PCI / 频段 / RSRP / RSRQ`；解析开关关闭时整张邻区卡片隐藏。
- **循环测速**：测速二级页增加“循环”时长，循环模式橙色提示并持续测速，直到用户点击停止；测速日志改为卡片呈现。
- **触控快速响应**：触摸队列扩大并改为普通 UI 动作只消费最新 tap/stroke，避免快速连滑后慢慢回放一串历史动作。

### 修复

- **浅色图表填充色**：折线图和测速图表的下方填充重新跟随曲线颜色，不再在浅色模式落成灰色。
- **二级页滚动与返回**：二级页使用独立返回动作和滚动状态，返回后不污染顶层页滑动位置。

### 文档

- 更新 [`docs/UI-GUIDE.md`](docs/UI-GUIDE.md)、[`docs/SPEEDTEST.md`](docs/SPEEDTEST.md)、[`docs/SIGNAL-CARDS.md`](docs/SIGNAL-CARDS.md) 和 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)，同步更多功能页、自定义功能目录、循环测速和邻小区显示规则。

### 验证

- 已在 ubuntu 编译机完成正式构建，发布资产 `u60pro-devui-aarch64` SHA256 `4bc5180923f0156a0848ad9dddb18b8732bb5e57069777e8e9af077aad9d210e`。
- 已推到 U60 实机验证：更多功能页、测速二级页、邻小区展开/隐藏和快速触控响应正常。

## v1.2.8 - 2026-07-08

### 修复

- **浅色模式测速仪表盘背景**：native gauge 绘制层现在跟随 `g_theme` 切换配色，浅色模式使用浅蓝白圆盘、深色刻度和深色测速数字，不再被深色圆盘覆盖。
- **测速折线图网格浅色适配**：折线图 native 网格线跟随浅色主题变浅，和浅色测速卡片背景保持一致。

### 验证

- 已在 ubuntu 编译机完成正式构建，发布资产 `u60pro-devui-aarch64` SHA256 `02172f7d5fbb5e45369805827247e2d4ccbc406c69dd812e9c72c33ba300fb69`。

## v1.2.7 - 2026-07-08

### 变更

- **锁屏预览隐藏测速 UI**：锁屏预览继续复用第一页实时信号卡片，但不再显示 `网络测速` 入口、展开面板或 native gauge/chart；测速后端检测和轮询仍保留，解锁后状态不滞后。
- **高铁/未激活标签视觉收口**：保留高铁专网卡片的紫色边框和左侧强调线，仅微调 `未激活`、`高铁专网` 标签的文字垂直位置。

### 修复

- **圆角卡片背景突出**：HTML 渲染器现在对 solid/gradient 背景统一按 `border-radius` 裁切，避免紫色高铁卡片角落露出方形背景。

### 文档

- 新增 [`docs/SIGNAL-CARDS.md`](docs/SIGNAL-CARDS.md)，整理第一页信号卡片、SCC 数据来源、未激活/高铁专网标签、锁屏预览和临时标签预览流程。
- 更新 [`docs/SPEEDTEST.md`](docs/SPEEDTEST.md)、[`docs/UI-GUIDE.md`](docs/UI-GUIDE.md) 和 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)，同步锁屏测速隐藏规则和 `{{SIGNALCARDS}}` 令牌口径。

### 验证

- 已在 ubuntu 编译机完成正式构建，发布资产 `u60pro-devui-aarch64` SHA256 `c01edfbc2480383677968e54fd545ec5aa06282ea435ba7ac0f4ea2afead1d9c`。
- 已推到 U60 实机验证：锁屏预览无测速入口，标签预览页显示正常，正式 `01-signal.html` 已恢复。

## v1.2.5 - 2026-07-04

### 变更

- **高铁专网载波提示改为白名单制**：第一页信号页不再把“非大网频点”一概染紫，而是仅在 `mcc = 460` 且 `EARFCN/ARFCN` 命中 `507150, 527070, 531390, 153370, 505230, 627744, 634464, 423630` 时，把对应 LTE/NR 载波卡片标成紫色并显示“高铁专网”。

### 文档

- **README / 开发文档同步当前高铁模式口径与版本示例**：补齐 `net.HSR` 与载波白名单提示的职责边界，并把 release 示例版本更新到 `devui 1.2.5 / ui 0.4.5`。

### 验证

- 已在 ubuntu 编译机重新完成正式构建，产出新的 `u60pro-devui.stripped` / `u60pro-devui-aarch64`。
- 已在 U60 实机验证：高铁模式主卡片紫色显示正常，白名单频点提示逻辑符合预期。

## v1.2.3 - 2026-06-29

### 变更

- **图表页功率改读电池而不是充电器**：历史功率曲线现在统一按 `battery.bat_uv * abs(battery.bat_ua)` 计算，避免插电场景下只看到充电器输入功率、却看不到电池真实充放电状态。
- **图表页功率卡片文案收敛**：右上角功率卡片直接显示 `充电/放电 4.3w` 这类最终语义，曲线本身不再额外依赖正负号区分。
- **构建脚本收敛到当前正式入口**：`scripts/build.sh` 现在直接产出 `u60pro-devui(.stripped)`，并按当前 HTML 渲染链路链接 `litehtml` / `FreeType`，不再把测试工具源文件一起误编进正式二进制。

### 文档

- **README / 开发文档同步当前发布产物**：构建、部署和发版说明统一改成 `bash scripts/build.sh`、`u60pro-devui.stripped` 与 release 资产 `u60pro-devui-aarch64`，不再沿用过时的 `html-poc` 表述。

### 验证

- 已在 ubuntu 编译机重新完成正式构建，产出新的 `u60pro-devui.stripped`。
- 已将新二进制和 `ui/05-charts.html` 推到设备 `/data/plugins/u60pro-devui/`，并确认设备侧进程已切换到新文件重启运行。

## v1.2.2 - 2026-06-26

### 变更

- **安装目录统一迁移到 `/data/plugins/`**：`devui` 固定安装到 `/data/plugins/u60pro-devui/`，界面模板固定安装到 `/data/plugins/u60pro-devui/ui`，后端固定安装到 `/data/plugins/zwrt-datad/`，并统一迁移/清理旧 `/data/u60pro` 与 `/data/ui` 残留。
- **`rc.local` 自启链路切到新路径**：稳定启动入口统一收敛到 `/data/plugins/u60pro-devui/start.sh`，后端进程名统一改为 `zwrt-datad`。
- **设备侧调试脚本同步到 HTTP/SSE**：辅助脚本不再依赖旧的 `state.json` 文件路径，统一以本机 `GET /state` 为准。

### 文档

- **README / 开发文档 / 更新日志补齐新目录规范**：当前安装方式、发布资产名和卸载说明全部统一到 `zwrt-datad` 与 `/data/plugins/...`。

### 验证

- 已在实机上完成目录迁移验证：正式运行路径改为 `/data/plugins/zwrt-datad/zwrt-datad`、`/data/plugins/u60pro-devui/u60pro-devui` 与 `/data/plugins/u60pro-devui/ui/`。
- 已确认新版 `start.sh`、`install-autostart.sh` 与插件目录布局一致，旧 `/data/u60pro` 可在安装时被清理。

## v1.2.0 - 2026-06-26

### 变更

- **前后端通信切到 `HTTP + SSE`**：`u60pro-devui` 不再读取 `/tmp/u60-datad/state.json`，改为先从 `GET /state` 拉当前快照，再通过 `SSE /events` 持续订阅后端完整状态。
- **状态刷新改为“live 快照 + 可见快照”双层缓存**：后台持续接收最新状态，页面只按设置页的 `refresh_ms` 把最新 live 状态提交到 UI，可继续保留 `停止 / 1s / 2s / 5s` 的节流语义。
- **交互动作优先读取 live 状态**：`NFC`、`ADB` 这类会依据当前状态做切换判断的操作，改为读取最新 live 快照，避免页面暂停刷新时仍按旧状态误判。

### 文档

- **README / 开发文档 / UI 教程统一改口到 HTTP/SSE 架构**：前端与后端的关系图、刷新机制说明和界面热更新描述，全部改为当前真实链路。

### 验证

- 已在实机上完成联调：正式 `zwrt-datad` 与 `u60pro-devui` 已替换为新版本。
- 已实测：`GET /state` 正常返回完整 JSON，`GET /events` 正常推送 `event: state`。
- 已实测：正式 `u60pro-devui` 与 `zwrt-datad` 在设备侧建立本机长连接，UI 正在消费 `127.0.0.1:9460` 的 SSE。

## v1.1.3 - 2026-06-25

### 变更

- **中国大陆四大运营商统一显示中文**：前端现在在读取 `state.json` 时就按 `MCC=460 + MNC` 统一正规化 `operator_name`，并兼容 `China Mobile / Unicom / Telecom / Broadnet` 等英文名回退到中文。
- **未激活载波纳入载波汇总与角标判定**：信号页的“已连接载波 / 总 MHz”统计现在按展示出来的载波卡片计算，未激活但已配置的载波也会计入；`5GA / 5G+ / 5G` 角标也与这套展示能力保持一致。
- **刷新间隔选项调整为四档**：设置页的“数据刷新”只保留 `停止 / 1s / 2s / 5s`，旧配置里若残留 `refresh_ms=500` 会在启动时自动归一到 `1000`。

### 修复

- **刷新间隔分段控件显示错乱**：修正设置页刷新间隔 segmented control 的布局与文字换行问题，避免轻微挤压时显示乱掉。
- **`0.5s` 与 `1s` 体感完全相同的误导选项**：当前设备侧 `u60-datad` 仍以 `-i 1000` 运行，前端 `0.5s` 只会更频繁检查 `state.json` mtime，不会带来更快的数据更新；该无效档位已移除。

## v1.1.2 - 2026-06-25

### 新增

- **设置页新增“数据刷新间隔”卡片**：系统页在开关区下方新增一张 segmented control 卡片，提供 `停止 / 0.5s / 1s / 2s / 5s` 五档轮询间隔，交互样式与自动息屏保持一致。

### 变更

- **状态轮询间隔改为可持久化配置**：`refresh_ms` 会写入 `/data/u60pro/devui.conf`，用户可以在“更实时”和“更省资源/更稳”之间自行取舍；`refresh_ms = 0` 时暂停状态轮询，但分钟级时钟仍继续刷新。

### 修复

- **短距离翻页回弹后邻页原生图表残留到当前页**：在锁频页等页面小幅左滑、预览下一页后回弹归位，之前可能因为命中 HTML 复用缓存而跳过整页重绘，导致图表页的 native draw 内容残留在当前页。现回弹结束后主动失效页面缓存，强制下一帧完整重绘。

## v1.1.1 - 2026-06-25

### 变更

- **自启安装脚本回到“vendor bring-up + `rc.local` 晚接管”的稳定链路**：`scripts/install-autostart.sh` 现在会清掉旧的重复钩子和实验性 `procd` 软链接，重新启用原厂 `zte_topsw_devui` 做早期屏幕/触摸 bring-up，再由 `/data/u60pro/start.sh` 完成交接。
- **`start.sh` 明确区分稳定默认路径与实验入口**：默认 `legacy` 模式继续用 `nohup` 拉起 `u60-datad` + `u60pro-devui`；`procd` 模式仍保留在脚本里，但只供手动实验，不再误当成默认发布路径。
- **页面重绘改为按状态快照变化驱动**：亮屏状态下不再每秒无条件整页重排，而是优先按 `state.json` mtime 变化与分钟跳变触发，减少无效刷新。

### 修复

- **短信列表打开目标改为稳定 ID 而非可变序号**：短信页点击动作从 `act:sms:N` 改成 `act:sms:ID`，避免列表排序、截断或插入新短信后点开错内容。
- **JSON key 扫描跨字符串误命中**：配套后端改进了 `json_get()` / `find_key()` 的字符串跳过逻辑，降低短信正文里含引号、逗号或类似 key 片段时读错字段的风险。
- **litehtml 页面重建后 RSS 持续爬升**：重载 HTML 时先 `reset_state()` 并释放旧 document，再创建新 DOM，避免长时间运行或频繁重排后内存一路抬升。
- **熄屏后首帧复用旧页面缓存**：灭屏时主动失效页面 HTML 复用缓存，避免 framebuffer 清黑后亮屏却因为“HTML 未变”而跳过重绘。
- **仅改 `style.css` 不触发整页重排**：页面缓存判定现在把 `style.css` mtime 一并纳入，CSS 改动会强制重新布局，不再沿用旧样式树。

### 验证

- 已在设备上以 `/tmp` 临时二进制做 smoke test，`u60pro-devui` 可正常拉起并接管 DRM / 触摸。
- 连续 `touch /data/ui/style.css` 6 轮时，UI RSS 约 `5272 KB -> 5968 KB`，未复现早期“从几十 MB 快速冲到 200 MB”的失控增长。
- 配合后端测试版运行时，设备侧 `state.json` 已命中 `sms` 相关字段，确认短信状态链路可以产出并被前端消费。

## v1.1.0 - 2026-06-25

### 新增

- **DevUI 内建 `DEVUI-IPC` 外部渲染接口**：新增本地 Unix Domain Socket `/tmp/u60-devui.sock` 与事件日志 `/tmp/u60-devui-events.log`。其他本机进程可通过 `PING`、`CLOSE`、`FRAME`、`IMAGE`、`DRAW`、`TEXT` 把像素帧、图片、绘图命令和文字页直接送进 DevUI，而不需要改 `/data/ui` 或自己接管 DRM/触摸。
- **外部画面正式接口文档**：新增 [docs/DEVUI-IPC.md](docs/DEVUI-IPC.md)，定义协议、生命周期、坐标系、事件格式和最小接入示例，作为后续扩展时的对外契约。

### 变更

- **外部接入改为“原生状态栏常驻 + 内容区接管”**：`DEVUI-IPC` 激活时保留顶部 `26px` 原生状态栏，由 DevUI 继续刷新时间、信号、电池和短信图标；外部内容固定显示在状态栏下方的 `320 x 454` 内容区。
- **文字/图片/像素帧统一落到内容区坐标系**：`FRAME` / `IMAGE` 自动缩放到状态栏下方内容区，`DRAW` / `TEXT` 的坐标与排版也统一以内容区左上角为原点，外部应用可以按固定尺寸布局，不必再手动避开状态栏。
- **DevUI 主循环内建外部画面前后台切换**：外部画面接管、超时返回、锁屏退出、电源键兜底退出都收进 DevUI 自己的主循环，不再需要额外修改原有 HTML 页面流或单独维护一套显示进程。

### 交互

- **新增系统级左边缘右滑返回**：外部画面前台时，用户可从内容区左边缘向右滑关闭当前外部画面并回到原生 DevUI；该手势属于系统保留行为，不写入事件日志。
- **外部触摸改为内容区点击回传**：外部画面前台时，内容区点击不再传给原生 HTML UI，而是按内容区坐标写入 `/tmp/u60-devui-events.log`，供外部应用自行做命中判断和重绘。

## v0.4.1 - 2026-06-24

### 变更

- **关机充电由 DevUI 直接接管**：停用“关机插电回到原厂充电动画”的路径，改为由 `u60pro-devui` 自己渲染全屏充电页。界面显示大号电池、电量百分比、充电状态，以及输入电压/电流；充电状态下不进入普通页面，也不依赖触控。
- **充电启动日志增强**：`start.sh` 记录 `mode_main_state`、`reboot_reason_code`、`/proc/cmdline`、电源 sysfs 和输入设备枚举到 `boot-trace.log`，便于后续继续排查不同固件/开机路径下的充电启动行为。

### 修复

- **0.3.9 的“关机插电不进 DevUI”在实机上未真正生效**：该固件的真实关机充电启动并不稳定表现为 `mode_power_off_*`，有时仍会落在 `mode_power_on`，甚至 `/proc/cmdline` 也带 `silent_boot.mode=nonsilent`。现改为双层兜底：启动脚本优先按 `mode_main_state` 判定，前端若检测到“外部供电 + 触控未初始化”则强制切到充电页，避免再次落入无触控的普通 DevUI。
- **关机充电页闪电图标只显示半个**：闪电改为非自交多边形并补投影，修复填充算法把图形吃掉一半的问题。
- **关机充电仍额外拉起 datad**：只有明确判定为正常开机时才启动 `u60-datad`；充电页自身直接读快照 / sysfs，减少离线充电态下不必要的后台进程。

### 验证

- 已实测：关机状态插电直接进入 DevUI 的全屏充电页，不再落回原厂无触控界面。
- 已实测：设备端替换新二进制后运行正常，`u60pro-devui` / `u60-datad` 启停符合预期。
- 已实测：抓取帧缓冲确认充电页闪电图标完整显示。

## v0.4.0 - 2026-06-24

### 变更

- **信号页 ENDC / LTE-NSA 结构调整**：ENDC 和 LTE-NSA 下改为 **NR 卡片 + LTE 卡片** 上下分离显示；NR only / LTE only 保持原布局。第一页内容超出一屏时可继续上下滚动。
- **ENDC / LTE-NSA 频宽汇总规则更新**：表头总频宽改为 **LTE + NR 相加**；LTE 子卡片额外显示 `X LTE 载波 · Y MHz` 摘要，便于直接看 4G 侧聚合情况。

### 修复

- **NR 主卡误显示 LTE 频段**：后端补回 `net.nr_band`，NR 主卡优先显示真实 `nr_band`，不再把 `wan_active_band` 错当 NR 频段。
- **QCI / AMBR、系统页、WiFi 页回归**：后端快照补回 `qos`、`system.cpu_usage`、`system.sw_version`、`system.imei`、`battery.chg_uv/chg_ua/bat_uv/bat_ua`、`wlan`、`nfc`、`dhcp`、`clients.list` 等字段，修复部署过程中引入的页面读空问题。
- **4G SCC 信号读取**：新增透传 `net.ltecasig`。当设备上的 `lteca` 为旧 5 字段格式，而 LTE 副载波的 `RSRP/SINR` 单独放在 `ltecasig` 时，UI 会自动补齐 4G SCC 的信号显示。
- **`ltecasig` 与 `lteca` 组数不一致**：实机存在 `lteca` 含 `PCell + 多个 SCC`、但 `ltecasig` 仅包含 `SCC` 组的情况。现 UI 自动从第二张 LTE 载波开始对齐 `ltecasig`，避免 LTE SCC 的 `RSRP` 全空、最后一个 `SINR` 丢失。

## v0.3.9 - 2026-06-19

### 修复

- **电源菜单跟随长页面滚动偏移**：设置页滑到底后长按电源，`menu.html` 会沿用当前页的 `g_scroll`，导致电源菜单整体被卷走、只剩「取消」露在顶部。现将 `menu.html` / `lockscreen.html` 这类特殊全屏页强制以 `scroll=0` 渲染，关闭菜单后普通页面仍保留原滚动位置。
- **关机插电应进入原厂充电动画**：原自启逻辑在 `/etc/rc.local` 中无条件停掉 `zte_topsw_devui` 并启动 DevUI，导致关机插电的离线充电动画被 DevUI 抢走。现自启先检查 `/proc/cmdline`，只有明确包含 `silent_boot.mode=nonsilent` 的正常开机才接管屏幕；离线充电启动保持原厂 `ANIM_MODE_POWER_OFF_CHARGER`。
- **管理插件更新 UI 后页面重复**：插件更新 `ui.tar.gz` 时会先解压并确认新包里有页面，再清空 `/data/ui` 顶层旧 `*.html` / `*.css`，最后复制新模板并核对数量，避免旧 4 页 + 新 4 页叠成 8 页；同时新增「重装UI」按钮，远端版本没变时也能手动重铺模板自愈。
- **短信状态栏图标错位/随滚动漂移**：未读短信信封图标改为按状态栏固定坐标绘制在时间右侧，不再依赖页面里的占位元素，长页面滚动时不会变位置。
- **短信页只能显示 6 条**：短信数据结构和列表缓冲扩大到 32 条，短信页内容超过一屏后可向下滚动查看。
- **页面滑动延迟和快速滚动卡顿**：屏幕亮着时主循环触摸轮询间隔从约 30ms 降到约 8ms，拖动起始阈值从 14px 降到 10px；竖向滚动帧只刷新状态栏下方内容区，不再每帧重画固定状态栏，减轻快速上下拖动时的卡顿。
- **亮屏淡入动画偏慢**：亮屏时背光淡入时长缩短约一半；息屏淡出时长保持不变。

### 验证

- 已实测：关机状态插电进入原厂充电画面；正常开机进入 DevUI。
- 已实测：流畅性优化版已部署到设备，进程正常接管 DRM/触摸，帧缓冲健康检查不是黑屏。
- 已实测：亮屏淡入减半版已部署到设备，`u60pro-devui` 正常启动。

## v0.3.8 - 2026-06-19

### 新增

- **USB-C 供电方向开关**：设置页新增「USB供电方向」，可在给 U60 充电和 U60 反向供电之间切换；网络共享开启时会走更稳的 Type-C/USB 顺序，避免开关状态来回弹。
- **USB 网络共享开关**：设置页新增「USB网络共享」，移除原设置页 ADB 开关。默认使用系统 `9057` composition（RNDIS : ECM），电脑侧走 RNDIS；手机侧若 `9057` 无 carrier，会自动降到纯 ECM `90B1`。
- **状态栏电量百分比开关**：默认开启，可在设置页隐藏/显示电池框内百分比。

### 变更

- **状态栏重绘**：时间靠左，网速靠近信号区；信号条、5G/5GA/5G+ 标识、电池框和电量百分比改为原生绘制并重新对齐。
- **状态栏常驻**：页面纵向滚动和横向切页时，状态栏保持钉在顶部，不再随内容消失又回来。
- **电池百分比可读性**：电量填充按百分比绘制，充电时填充为绿色；百分比文字根据背景实时选择高对比颜色。

### 修复

- **电脑/手机 USB 网络自适应**：修复从手机 ECM 切回电脑时粘在 `90B1/ECM` 的问题；watchdog 会检测 `ecm0/rndis0 carrier`，必要时清掉残留 source 标记并回到 `9057/RNDIS`。
- **手机反向供电 + ECM 共享**：按实测顺序先建立 ECM carrier，再切 `source/device` 和 powerbank，保持 U60 给手机供电的同时向手机共享网络。
- **设置页语言和排版回归**：恢复中国运营商中文显示相关文档记录，修正设置页新增开关后的布局说明。

### 说明

- 跳过 `v0.3.7`：历史里有过一次 `v0.3.7` 发布提交，但当前 `main` 已回滚，GitHub latest release 仍是 `v0.3.6`；本次直接发布 `v0.3.8`。
- ADB 路径这次只做分析记录，不恢复设置页 ADB 开关。

## v0.3.6 - 2026-06-16

### 变更

- **电源菜单改版（长按电源键弹出）**：原来上半屏三个长方形开关，改为**三个竖排大圆形按键**，圆盘和图标全部原生绘制（litehtml 画不了这些符号）——关机=电源符号（红）、重启=圆箭头（橙）、取消=X（灰），每个圆下方标文字。二次确认保留：第一次点亮白圈光环、标签变「再按一次」。

### 修复

- **WiFi 节能模式开关开了不生效/会自己关闭**：`power_save` 需要靠 `/etc/hotplug.d/iface/99-disable-powersave` 这类 hotplug 脚本在每次 `ifup` 时重新应用，否则切频段/重连/重启后会被弹回默认（关闭）。原本该脚本要手动安装、且「节能 ON」只是一次性 `iw set on`，导致没装脚本或开启节能的用户都不能保持。现改为**开关自己把脚本 echo 到 `/etc/hotplug.d/iface/99-disable-powersave`**（按所选模式在 ifup 时重新应用 on/off），功能自包含、无需另行安装；同时清掉旧版遗留的 `psm` 文件。

## v0.3.5 - 2026-06-15

### 新增

- **短信查看（只读）**：新增第二页「短信」，按时间倒序列出最近 6 条。列表为**折叠卡片**——每条只显示号码 / 时间 + 一行正文预览；**点卡片弹出二级页面**看完整内容，点「关闭」或弹窗外区域返回。只读，不支持发送。后端走厂商 `zwrt_wms`：未读数读 `zwrt_wms_get_wms_capacity`，列表读 `zte_libwms_get_sms_data`；正文是 UTF-16BE 十六进制，后端解码成 UTF-8（含 emoji 代理对），日期格式化成 `MM-DD HH:MM`。令牌 `SMSLIST`，未读条目左侧红点 + 蓝色号码高亮。
- **点开短信自动标为已读**：进入二级页面时，若该条未读则调用 `zwrt_wms_modify_tag {id, tag:0}` 标为已读；未读数变化后后端立即刷新列表，红点与状态栏图标随之消失。
- **状态栏未读短信图标**：检测到未读短信时，时间右侧**原生绘制一个信封图标**（蓝色信封 + 白色翻盖，非字体字形）。状态栏置顶、翻页时也常驻。**锁屏状态栏同样显示**（锁屏复用 `STATUSBAR`）。

### 变更

- **锁频二级弹窗频段芯片一行四个**：原 3 列改 4 列。弹窗内宽约 264px，芯片 56→52px、外边距 4→3px、字号 15→14px，确保四个并排不换行。
- **页面重新编号**：原 02-wifi / 03-lock / 04-charts / 05-system 顺延为 03 / 04 / 05 / 06，短信页插入为 02。

### 注意

- 短信正文可能含任意字符，UI 端对 `&<>` 做了 HTML 转义后再渲染。

## v0.3.4 - 2026-06-15

### 新增

- **电源直供电开关**（第五页）：插电时直接外部供电、不经电池充放，减少长期插电的电池损耗。走厂商 `zwrt_bsp.charger`（`set {direct_power_supply_mode:enable/disable}`，状态读 `list`）。令牌 `DPSCLASS/DPSSTATE`，动作 `act:dps`。
- **锁屏改版**：锁定后先显示**第一页实时预览**（数据刷新但不可操作）+ 屏幕中下方一个**原生锁图标**（随主题黑/白）；任意方向**滑动**才进 PIN 键盘。
- **电源菜单二次确认**：长按电源的关机/重启改为**点一次→按钮变「再按一次…」+白色描边（点击反馈）→再点执行**；执行时**先熄屏再发命令**（关机/重启有延迟，先黑屏给即时反馈）；4 秒无第二次点自动复原。
- **亮度持久化**：屏幕亮度写入 `/data/u60pro/devui.conf`，开机恢复、重装不丢。

### 修复

- **熄屏仍响应触摸（安全）**：触摸锁存队列在熄屏时照常入队，熄屏期间的点击会在亮屏后**回放**——曾导致熄屏时盲点也能把 PIN 输进去。现熄屏**完全忽略触摸并清队列**，亮屏首帧丢弃唤醒触摸。
- **锁频二级弹窗频段不清晰**：芯片颜色规则错挂在 `body.dark/.light`（弹窗 body 是 `mo`，从未命中）导致未选中项落到默认蓝字。改挂 `.modal.dark/.light`：未选中=边框轮廓+黑/白字，选中=蓝底白字。
- **已连接设备列表**：不再折叠（全部展开）；按**当前在线**过滤（WiFi 关联 ∪ ARP 在线），去掉 DHCP 租约残留的离线设备，且不误删 LAN/USB 接入设备。

### 移除

- **双击点亮**：连同熄屏触摸一并移除（熄屏不再检测触摸，唤醒只用电源键）。

## v0.3.3 - 2026-06-14

### 新增

- **锁屏（4 位 PIN）**：第五页设置卡新增「锁屏」开关。
  - 开关从关→开时进入**二级 PIN 键盘**设置 4 位密码（带「删除」键、无确认键，输完第 4 位自动保存并开启）；开→关直接清除密码、关闭锁屏。设置中可点「取消」放弃。
  - 开启后**屏幕超时或按电源键即自动锁屏**；开机若已设密码也直接进入解锁键盘。
  - 解锁键盘输满 4 位**自动校验**：正确进入界面，错误在**屏幕中间弹出「密码错误」**并清空重输。锁定时电源键长按不再弹电源菜单。
  - PIN 存于 `/data/ui/.lockpin`（持久、不被推送 `*.html` 覆盖）。忘记密码可 `adb shell rm /data/ui/.lockpin` 解除。
  - 新增界面文件 `lockscreen.html`（特殊页，不计入翻页）；令牌 `LOCKTITLE/PINDOTS/LOCKMSG/LOCKAUX`，动作 `locktoggle/pin:<0-9>/pin:del/lockcancel`。
- **真·圆角渲染**：之前自写的 litehtml 容器 `draw_solid_fill`/`draw_borders` 直接画矩形、忽略 `border-radius`，所以整个 UI 的圆角在设备上从未真正出现。新增 `fill_rounded`（圆角填充）与 `stroke_rounded`（等宽圆角描边），卡片/开关/按钮/电池外框等终于是真圆角。另加 `html_view_fill_poly`（扫描线多边形填充）。
- **充电闪电图标**：去掉原来的白色流光动画，改为充电时在电池上**原生绘制闪电**（随主题黑/白、贯穿电池上下、不超出状态栏）；电池压扁一点。
- **第二页 WiFi**：新增 **2.4G / 5G 频段开关**与 **WiFi 节能模式（PSM）开关**。频段用纯 `ifconfig wlanN up/down` 控制、读 `operstate`（本固件 uci/`wifi reload` 无效）；PSM 用 `iw set power_save` + hotplug 脚本持久化。令牌 `WIFI24*/WIFI5*/PSM*`，动作 `wifi24/wifi5/psm`。
- **设置持久化**：主题 / 速率单位 / 双击点亮 / 自动息屏 存到 `/data/u60pro/devui.conf`，开机加载、改动即写，重装不丢。
- **DHCP 地址池实时化**：改为 UI 直接由 uci（`network.lan.ipaddr` + `dhcp.lan.start/limit`）算出地址池范围，跟随实际网段。

### 修复

- **快速点击被吞**：触摸层新增点击边沿锁存 + 队列（`touch_input_take_tap`），一次完整按下→抬起若落在慢渲染期间也不再丢，锁屏键盘连点不掉键。
- **翻页时状态栏割裂**：横滑翻页时**钉住顶部状态栏**（只滑下方内容），闪电随之对齐、不再先消失再出现。
- **第四页网速单位写死**：右上角 ↓/↑ 数值原本恒为字节率（MB/s）、无视「速率单位」设置，现已跟随（Mbps/MB/s）。
- **第五页电池电流标正负**：充电 `+`、放电 `-`（与「充电/放电」标签一致）。

### 调整

- 设置页开关（`.sw`）整体放大并与文字**垂直居中**对齐（行改 table 布局）。

## v0.3.2 - 2026-06-14

### 新增

- **熄屏/亮屏淡入淡出**：熄屏时背光从当前亮度平滑降到 1 再灭、亮屏从 0 渐亮到熄屏前亮度；时长按亮度比例缩放（满亮度各约 0.125s）。淡变只改实时背光，**不影响第五页设置的亮度**（`backlight_get()` 返回记忆的用户亮度）。
- 亮屏前在背光为 0 时先把命令模式面板"热身"几帧，避开退出 idle 的瞬态闪烁（中高亮度已无闪）；并加最低档软启动以减轻低亮度过冲。

### 说明

- 淡变用真实时钟控时（写背光 sysfs 较慢，按步数计时会超时）。
- 极低亮度/偶发熄屏的轻微闪是背光升压电路冷启动/关断的硬件瞬态，纯软件难以根除。

### 新增

- **第五页系统卡**新增充电器/电池电压电流（充电器一行、电池一行，各两列，位于内存上方）。
- **第四页新增电池图表**：充电功率折线 + 电池温度折线，右上角显示温度与功率数值；**未充电时显示放电功率**（电池 V×I）。
- 后端 `u60-datad` 新增 `battery.chg_uv/chg_ua/bat_uv/bat_ua`（充电器/电池电压µV、电流µA，读自 `power_supply` sysfs）。

### 调整

- **自动息屏**改为与选网方式一致的**分段控件**（关/30秒/1/2/5/10分，蓝色高亮跟手滑动 + 单击）。
- 分段控件单元格显式等宽（litehtml 不自动平分 `table-layout:fixed`）。
- 删除可滚动页右侧的滚动条指示（滚动功能保留）。

## v0.3.0 - 2026-06-14

### 新增

- **新增图表页**：CPU 占用率+温度同图双色折线、内存占用率折线、网速上下行双色折线（单位跟随设置）。折线用 Bresenham 原生绘制到占位框（litehtml 无 canvas），UI 端保留 48 秒滚动历史、每秒采样。
- **新增锁频页**（第 3 页）：
  - 顶部「选网方式」**分段控件**（自动 / 5G SA / 5G NSA / 4G），蓝色高亮原生绘制、跟手滑动、松手吸附并应用；单击亦可。值映射：自动=`WL_AND_5G`、5G SA=`Only_5G`、5G NSA=`LTE_AND_5G`、4G=`Only_LTE`。
  - **5G SA / 5G NSA / 4G 三张锁频卡**，卡片底部显示当前锁定频段；点开弹出**二级弹窗**（约 90% 屏幕、背景变暗仍可见、状态栏留空、禁操作一级菜单）。弹窗内频段一行四个居中排列，底部「全选/全不选 · 反选 · 应用」；应用后弹出居中**提示**并回到一级菜单。
  - 底部「恢复默认配置」一键解锁并恢复默认（`nwinfo_reset_band_cell_setting`）。
  - 底层用厂商 API：`nwinfo_set_netselect`、`nwinfo_set_nrbandlock`(type 0=SA/1=NSA)、`nwinfo_set_lte_ext_band`（逗号频段列表）。
- **WiFi 页**新增：WiFi 开关、NFC 碰一碰开关、已连接设备列表（名称/IP，来自 DHCP 租约）、DHCP 信息（网关/地址池/租期）。
- **系统页**：删除小区 PCI；CPU/内存显示占用率（内存附 `已用/总 MB`）、CPU 温度、电池温度；系统版本号、IMEI（默认打码）；**屏幕亮度滑条**（点/拖直接调背光）、**自动息屏**预设（关/30秒/1/2/5/10分，当前项高亮）、**双击点亮**开关、ADB / 速率单位 / 界面主题开关。
- **长页面竖向滚动**：内容超一屏时可上下拖动（预渲染整页到缓冲再窗口贴图，跟手流畅），右侧有滚动条。
- **自动息屏 + 双击唤醒**：无操作超时自动关背光，双击屏幕唤醒（可开关）。
- 渲染器新增原语：原生折线、占位框定位（`get_placement`）、整页高度（修正 `render()` 返回宽度的坑）、覆盖层渲染（modal）、矩形/滚动条绘制、竖向滚动偏移。
- 后端 `u60-datad` 新增字段：`system.cpu_usage`/`mem_total`/`mem_avail`/`sw_version`/`imei`、`clients.list`、`wlan.enabled`、`nfc.switch`、`dhcp.*`、`net.net_select`/`sa_bands`/`nsa_bands`/`lte_bands`；qci/ambr 日志窗口扩大并缓存最后已知值。

### 调整

- **信号页载波显示按组网模式重写**：表头随 `5G SA / 5G NSA / EN-DC / 4G` 变化，显示「L LTE + M NR 载波 · 总 X MHz」；4G/NSA 下 LTE 与 NR 载波都显示，LTE 频段/EARFCN/PCI 解析自 `lteca`。频宽规则：SA=NR、4G=LTE、NSA=有 NR 取 NR 否则 LTE（不相加）、EN-DC=LTE+NR 相加。
- 充电流光动画裁剪到电池框内（`overflow:hidden`），不再越过边框。
- ARFCN/EARFCN 移到信号页每张载波卡右上角（PCI 正上方）；系统页 ARFCN 行改为 Cell ID（默认打码 + 点击显示）。
- 运营商对中国大陆四家显示中文（中国移动 / 联通 / 电信 / 广电）。
- 页面从 3 页增加到 5 页：信号 / WiFi / 锁频 / 图表 / 系统（+电源菜单）。

### 修复

- 选网分段控件松手/单击后高亮回弹（改用乐观 pending，高亮立即停在所选模式直至模组切换完成）。
- 纯 SA 误显示 LTE 载波；NSA 锁频可用频段被启动瞬时子集带偏（改为取历来最大集合，已选实时跟随模组）。
- 系统页 CPU 占用率/版本号/IMEI 读取错误（`data.c` 漏解析）。
- WiFi 页内容超出被裁、第四页底部开关被翻页圆点遮挡（加底部留白）。

## v0.2.0 - 2026-06-13

这一版是一次**架构级改动**：界面从内置的 LVGL 代码改为**运行时渲染 HTML/CSS**。程序变成固定的"框架"，真正的界面是 `/data/ui` 目录里的 HTML 文件，用户改界面不用再重新编译。详见新增的 [docs/UI-GUIDE.md](docs/UI-GUIDE.md)。

### 架构

- **改用 litehtml + FreeType 渲染 HTML/CSS** 到 RGB565 framebuffer，全静态链接 aarch64 musl 单文件。
- 界面与程序解耦：`/data/ui/NN-名字.html` 每个文件一页，`style.css` 共享样式，`menu.html` 为电源菜单。
- HTML 里的 `{{令牌}}` 在渲染前由程序替换为实时数据；`href="act:xxx"` 为交互动作。每次刷新重读文件，改完即时生效。
- 后端从 `zwrt-datad` 切换为 `u60-datad`，快照路径统一为 `/tmp/u60-datad/state.json`。

### 新增

- **信号页**：按载波分卡片显示 频段·频宽 / PCI / RSRP / SINR，按质量上色；未激活副载波（RSRP 为 -140 哨兵值）置灰并标"未激活"。
- **状态栏**：时间 · 实时网速(`↑上 ↓下`，单位可切) · 网络代际文字(5GA/5G+/5G/4G/LTE/3G) · 格数信号强度 · 电池(带充电流光) · 电量。
- **WiFi 页**：SSID、密码（默认打码，点按才明文）、加密方式、设备数。
- **系统/设置页**：速率流量、PCI、ARFCN（默认隐藏）、型号/运行/温度/内存，以及 ADB 调试 / 速率单位 / 界面主题三个开关。
- **网络代际徽章**：按 MCC/MNC + 载波聚合判定 5GA / 5G+ / 5G / 4G / LTE / 3G。
- **运营商中文名**：中国移动 / 联通 / 电信 / 广电。
- **QCI / AMBR**：从后端 QoS 读取并显示（AMBR 统一 Mbps）。
- **跟手翻页动画**、**充电流光动画**（宿主驱动帧，绕过 litehtml 无 CSS 动画的限制）。
- **底部圆点**翻页指示。
- 文档：新增《自定义界面教程》[docs/UI-GUIDE.md](docs/UI-GUIDE.md)；DEVELOPMENT.md 全面重写为 HTML 架构。

### 修复

- 纯 NR-SA 下误显示 LTE 载波（modem 残留 lte_rsrp）。
- 后端补回/新增 `nrca`/`lteca`/`usb_mode`/`wlan` 字段（缺 `wlan` 会导致 WiFi 页读空）。
- 状态栏制式徽章背景色因裸 class 名泄漏到纯文字 span（紫色框），改为带 `.gen` 前缀。
- 状态栏文字与 inline-block 元素对齐错位；电池正极脱节。

### 安全

- WiFi 密码、ARFCN 默认打码，需显式点按才明文显示。

## Unreleased（LVGL 时期）

### 新增

- 新增 WiFi 页面，可显示主 WiFi 的 SSID、密码、加密方式，并生成可扫码加入的二维码。
- 新增开机自启辅助脚本，便于把 `u60pro-devui` 和后端一起放到设备启动流程里。
- 文档中补充了默认后端依赖关系，明确 `u60pro-devui` 默认配套 `zwrt-datad` 仓库的 `u60pro` 分支。

### 调整

- 后端数据读取路径改为 `/tmp/zwrt-datad/state.json`，与 `zwrt-datad` 的 `u60pro` 分支保持一致。
- 信号区改成分段式指示样式，页面圆点也从两页扩展为三页。
- `data.h` 扩展了 WiFi 相关字段，便于 UI 直接消费后端快照。
- `README.md` 和 `docs/DEVELOPMENT.md` 补充了后端分支引用说明。

### 修复

- UI 不再默认依赖旧的 `u60-datad` 路径，避免和当前后端分支不一致。
- 将 UI 和后端关系写入文档，减少后续维护时的歧义。

## 2026-06-12

### 初始导入

- 建立 clean-room 的 U60Pro 前面板 UI 项目。
- 接入 LVGL + DRM/KMS + evdev 的基础显示、触摸和页面框架。
- 增加与 `zwrt-datad` 后端配套的快照读取模型。
